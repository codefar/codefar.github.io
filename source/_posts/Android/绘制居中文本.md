
# 绘制居中文本

```
// 文本宽
canvas.save();
//移动到绘制中心
canvas.translate(mBgBounds.centerX(), mBgBounds.centerY());
// 文本baseline在y轴方向的位置
float baseLineY = Math.abs(mTextPaint.ascent() + mTextPaint.descent()) / 2;
canvas.drawText(str, -textWidth / 2, baseLineY, mTextPaint);
canvas.restore();
```